import vtk
import numpy as np

def plot_sphinx(verts: np.array, faces: np.array, filename: str):
    """make an image of the sphinx

    Args:
        verts (np.array): sphinx vertices
        faces (np.array): sphinx faces
        filename (str): filename to save plot
    """
    return None